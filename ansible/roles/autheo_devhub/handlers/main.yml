---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart autheo-devhub
  ansible.builtin.systemd:
    name: autheo-devhub
    state: restarted
    enabled: true
    daemon_reload: true
